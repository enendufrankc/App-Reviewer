[build]
builder = "dockerfile"

[environments.production]
variables = {}

[[services]]
name = "backend"
source = "backend/"

[services.build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[services.variables]
PORT = "8000"

[[services]]
name = "frontend"
source = "frontend/"

[services.build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[services.variables]
PORT = "80"